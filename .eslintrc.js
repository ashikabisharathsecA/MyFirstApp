module.exports = {
  root: true,
  extends: ['@react-native'],
  parserOptions: {
    ecmaVersion: 2020,
  },
  rules: {
    // Add custom rules here if needed
    // Example: "no-console": "warn"
  },
};